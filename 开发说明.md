# 🎮 游戏开发说明文档

## 📋 项目概述
这是一款基于 Unity 6.4 引擎开发的 2D 横版冒险游戏。

---

## 🛠️ 我的开发工作内容

### 一、玩家控制系统（核心编程）

#### 1. 基础移动系统
- **文件位置**: `Assets/C# script/player.cs`
- **技术实现**:
  - 使用 Unity 新输入系统（`UnityEngine.InputSystem`）
  - 通过 `Rigidbody2D` 实现物理运动
  - 支持键盘 A/D 和方向键控制

#### 2. 二段跳系统 ✨（新增功能）
- **功能描述**: 玩家在空中可以进行第二次跳跃
- **核心逻辑**:
  ```csharp
  private int jumpCount = 0;           // 跳跃计数
  private int maxJumpCount = 2;        // 最大跳跃次数
  
  // 落地时重置跳跃计数
  if (IsGrounded()) {
      jumpCount = 0;
  }
  
  // 普通跳跃 + 二段跳
  if (keyboard.spaceKey.wasPressedThisFrame) {
      if (IsGrounded()) {
          Jump();
          jumpCount = 1;
      } else if (jumpCount < maxJumpCount) {
          DoubleJump();
          jumpCount++;
      }
  }
  ```

#### 3. 冲刺系统 ✨（新增功能）
- **功能描述**: 按左 Shift 键可以快速冲刺，有冷却机制
- **技术亮点**:
  - 状态管理（是否正在冲刺、是否可以冲刺）
  - 时间管理（冲刺持续时间、冷却时间）
  - 优先级控制（冲刺状态优先于普通移动）

#### 4. 动画状态机
- 定义 `MovementState` 枚举：`stay`, `running`, `jump`, `fall`, `dash`
- 根据输入和物理状态动态切换动画

---

### 二、游戏状态管理系统

#### 1. 单例模式的 GameManager
- **文件位置**: `Assets/C# script/GameManager.cs`
- **核心功能**:
  - 全局唯一实例（单例模式）
  - 跨场景数据持久化（`DontDestroyOnLoad`）
  - 管理生命值、分数、已收集物品

#### 2. 生命值与分数系统
- 实时更新 UI 显示（`LivesDisplay.cs`）
- 收集物去重机制（`HashSet` 记录已收集物品）

---

### 三、敌人AI系统 ✨（新增功能）

#### 1. 巡逻敌人
- **文件位置**: `Assets/C# script/Enemy.cs`
- **功能特性**:
  - 在两个巡逻点之间来回移动
  - 到达目标点后短暂停留
  - 自动翻转朝向

#### 2. 战斗系统
- **碰撞机制**:
  - 玩家正面碰到敌人 → 玩家死亡
  - 玩家从上方跳到敌人头顶 → 消灭敌人并获得分数

---

### 四、陷阱与关卡系统

#### 1. 多样化陷阱
- **地刺陷阱** (`Trap1.cs`): 触碰即死亡
- **移动陷阱** (`trapmove.cs`): 在两点间移动
- **移动平台** (`moveisland.cs`): 玩家跟随平台移动
- **旋转障碍物** (`rotate.cs`): 持续旋转的障碍

#### 2. 关卡流程
- 主菜单 → 关卡1 → 关卡2 → 胜利/失败场景
- 终点旗帜检测与场景切换

---

### 五、摄像机系统

#### 摄像机跟随
- **文件位置**: `Assets/C# script/CameraFollow.cs`
- 实时跟随玩家位置
- 保持正交视图大小固定

---

## 🎯 技术亮点（面试重点）

### 1. 设计模式
- **单例模式**: `GameManager` 确保全局唯一实例
- **状态机模式**: 玩家动画状态管理

### 2. 物理系统
- `BoxCast` 地面检测
- `Rigidbody2D` 物理运动
- 父子关系实现平台跟随

### 3. 数据结构
- `HashSet` 实现高效的收集物去重
- 枚举类型管理状态

### 4. 输入系统
- 使用 Unity 新输入系统替代旧系统
- 支持多种输入设备

---

## 🎮 操作说明

| 按键 | 功能 |
|------|------|
| A / ← | 向左移动 |
| D / → | 向右移动 |
| 空格 | 跳跃（支持二段跳） |
| 左 Shift | 冲刺 |

---

## 📁 代码文件清单

| 文件名 | 功能描述 | 工作量占比 |
|--------|----------|-----------|
| `player.cs` | 玩家控制、二段跳、冲刺 | ⭐⭐⭐⭐⭐ |
| `GameManager.cs` | 全局状态管理 | ⭐⭐⭐⭐ |
| `Enemy.cs` | 敌人AI系统 | ⭐⭐⭐⭐ |
| `Collectible.cs` | 收集物系统 | ⭐⭐⭐ |
| `PlayerDeath.cs` | 玩家死亡逻辑 | ⭐⭐ |
| `CameraFollow.cs` | 摄像机跟随 | ⭐⭐ |
| `flag.cs` | 关卡终点 | ⭐⭐ |
| `trapmove.cs` | 移动陷阱 | ⭐⭐ |
| 其他脚本 | UI、菜单、简单陷阱 | ⭐⭐ |

---

## 💡 验收展示建议

### 1. 先展示新增功能
- 演示二段跳
- 演示冲刺系统
- 演示敌人AI

### 2. 再讲解核心逻辑
- 打开 `player.cs`，展示二段跳和冲刺的代码
- 打开 `GameManager.cs`，讲解单例模式和状态管理
- 打开 `Enemy.cs`，展示敌人AI逻辑

### 3. 强调技术亮点
- 新输入系统的使用
- 状态管理机制
- 物理碰撞检测

---

**总结**: 本项目虽然使用了商店素材和预制动画，但核心的游戏逻辑、状态管理、输入处理、AI行为等都是由我独立编写的代码实现的。
